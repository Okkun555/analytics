import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#00c4cc",
      contrastText: "#FFFFFF",
    },
    secondary: {
      main: "#0070C7",
      contrastText: "#000000",
    },
  },
});

export default theme;
